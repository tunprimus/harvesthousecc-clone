image: alpine
pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
      - public
  only:
  - main
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
